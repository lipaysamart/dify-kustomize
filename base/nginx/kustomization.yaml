resources:
  - deployment.yaml
  - service.yaml
labels:
  - pairs:
      app.kubernetes.io/name: dify-nginx
    includeSelectors: true
configMapGenerator:
  - name: dify-nginx
    files:
      - nginx.conf=nginx.conf
